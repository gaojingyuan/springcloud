spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: development
      discovery:
        enabled: true
        serviceId: config-server
server:
  port: 8012
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/
