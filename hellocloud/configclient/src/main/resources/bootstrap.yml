spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: development
      discovery:
        enabled: true
        serviceId: config-server
  rabbitmq:
    host: localhost
    port: 15672
#    username:
#    password:
server:
  port: 8012
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/
